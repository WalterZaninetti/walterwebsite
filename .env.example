# The natural-language-to-scryfall-filters service backing /magic-tools.
# Defaults to the local dev server when unset. In production, point this at
# the deployed instance and add that origin to the service's ALLOWED_ORIGINS.
VITE_TRANSLATE_API_URL=http://localhost:8080
